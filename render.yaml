services:
  - type: web
    name: forge-api
    env: docker
    dockerfilePath: ./apps/api/Dockerfile
    healthCheckPath: /health
    autoDeploy: true
  - type: worker
    name: forge-worker
    env: docker
    dockerfilePath: ./apps/worker/Dockerfile
    autoDeploy: true
